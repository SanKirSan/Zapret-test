These files are launchers/reference helpers only. They are not standalone copies of the upstream projects.
zapret-test runs the real blockcheck.sh from the installed zapret tree and the real blockcheck2.sh from the installed zapret2 tree.
