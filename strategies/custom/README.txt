Custom strategies are deliberately not generated by the program.
Use a simple file per strategy and keep the exact argument string known to your installed binary.

Example:
ID=MY-DISCORD-001
ENGINE=ZAPRET2
PROTOCOL=HTTPS
DESCRIPTION=User supplied, previously observed strategy
SOURCE=user
COMMAND=--filter-tcp=443 --filter-l7=tls --payload=tls_client_hello ...
